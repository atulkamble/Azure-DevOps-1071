# 📘 GitHub Repository – Clone & Maintain Guide

This document explains how to create a GitHub repository, clone it locally, and maintain it using basic Git commands.

---

## 1️⃣ Login to GitHub

* Open 👉 [https://github.com](https://github.com)
* Login with your GitHub credentials

---

## 2️⃣ Create a New Repository

1. Click **➕ (Top Right Corner)**
2. Select **New Repository**
3. Enter:

   * Repository Name: `myrepo`
   * Visibility: Public / Private
4. Click **Create Repository**

---

## 3️⃣ Clone Repository to Local Machine

After creating the repo:

1. Click **Code**
2. Copy the HTTPS URL

Example:

```bash
git clone https://github.com/atulkamble/myrepo.git
cd myrepo
```

---

## 4️⃣ Create a New File

```bash
touch helloworld.py
```

---

## 5️⃣ Add File to Staging

```bash
git add helloworld.py
```

> ⚠️ Make sure filename spelling matches exactly.

---

## 6️⃣ Commit Changes

```bash
git commit -m "added code"
```

---

## 7️⃣ Push Code to GitHub

```bash
git push origin main
```

When prompted:

* **Username:** `atulkamble`
* **Password:** Paste your **Personal Access Token (PAT)**

⚠️ GitHub does NOT accept account password for CLI authentication.
You must use a **Personal Access Token (PAT)**.

---

## 8️⃣ Create Personal Access Token (Classic)

Steps:

1. Go to GitHub
2. Click **Profile → Settings**
3. Scroll to **Developer Settings**
4. Click **Personal Access Tokens**
5. Select **Tokens (Classic)**
6. Click **Generate New Token (Classic)**
7. Select required scopes:

   * ✅ repo
   * ✅ workflow (optional)
8. Click **Generate Token**
9. Copy and store securely

Example Token format:

```
ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

⚠️ Never share your token publicly.

---

## 9️⃣ Pull Latest Changes

To fetch updates from remote repository:

```bash
git pull origin main
```

---

# 🔁 Daily Maintenance Workflow

```bash
git pull origin main
git add .
git commit -m "your message"
git push origin main
```

---

# 📌 Common Git Commands

| Command                      | Purpose             |
| ---------------------------- | ------------------- |
| `git status`                 | Check file status   |
| `git log`                    | View commit history |
| `git branch`                 | View branches       |
| `git checkout -b branchname` | Create new branch   |
| `git merge branchname`       | Merge branch        |
| `git remote -v`              | Check remote URL    |

---

# 🔐 Best Practice (Recommended)

Instead of using token every time:

```bash
git config --global credential.helper store
```

Or use GitHub CLI:

```bash
gh auth login
```

---

# ✅ Summary

✔ Create repo
✔ Clone repo
✔ Add file
✔ Commit
✔ Push
✔ Pull updates
✔ Use Personal Access Token for authentication

---
